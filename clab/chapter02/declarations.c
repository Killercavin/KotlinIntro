#include <stdio.h>
#include <string.h>

int main() {
    int lower, upper, step;
    char c, line[1000];
    
    // string declaration
    char my_string[] = "";

    int string_length = strlen(my_string);

    printf("%d\n", string_length);

    return 0;
}