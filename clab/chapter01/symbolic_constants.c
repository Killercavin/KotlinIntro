#include <stdio.h>

#define LOWER 0 /** lower limit */
#define UPPWR 300 /** upper limit */
#define STEP 20 /** step size */

int main() {
    int temp_celsius;

    for(temp_celsius = LOWER; temp_celsius <= 300; temp_celsius + STEP) {
        /** code */
    }

    return 0;
}