package errors

// These are errors that occur preventing the program from running

fun main() {
    println("These lines below have compile time errors") // example of compile-time error

    // printn("Hello) // printn missing l, missing the closing quote
}